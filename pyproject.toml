[project]
name = "resume-helper"
version = "0.1.0"
description = "This is the resume helper using prompt engineering"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai-agents>=0.17.4",
]
